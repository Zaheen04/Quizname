// userReducer.js

const initialState = {
    // Initial state for user
  };
  
  const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define cases for user actions
      default:
        return state;
    }
  };
  
  export default userReducer;
  