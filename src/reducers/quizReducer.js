// quizReducer.js

const initialState = {
    // Initial state for quiz
  };
  
  const quizReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define cases for quiz actions
      default:
        return state;
    }
  };
  
  export default quizReducer;
  